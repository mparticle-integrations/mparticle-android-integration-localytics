buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
    }
    dependencies {
        classpath 'com.mparticle:android-kit-plugin:+'
    }
}

apply plugin: 'com.mparticle.kit'

dependencies {
    compile files('libs/localytics400.jar')
    testCompile  files('libs/java-json.jar')
}